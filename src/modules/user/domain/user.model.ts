export type User = {
  allowed: boolean;
  email: string;
  id: string;
  name: string;
  owner: boolean;
  photoUrl: string;
  tenantId: string;
};

export type UserFirebase = {
  allowed: boolean;
  email: string;
  id: string;
  name: string;
  owner: boolean;
  photoUrl: string;
  tenantId: string;
};
